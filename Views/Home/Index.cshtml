@{
    ViewData["Title"] = "Home Page";

    /**
// Verificar conexión a la base de datos
using (var scope = Context.RequestServices.CreateScope())
{
var dbContext = scope.ServiceProvider.GetRequiredService<ApplicationDbContext>();
if (dbContext.Database.CanConnect())
{
ViewData["DbConnectionMessage"] = "Conexión a la base de datos exitosa.";
}
else
{
ViewData["DbConnectionMessage"] = "Error al conectar a la base de datos.";
}
} **/
}

<div class="d-flex justify-content-center align-items-center vh-100"
    style="background: linear-gradient(135deg, #1e3c72, #2a5298); color: white; font-family: 'Arial', sans-serif;">
    <div class="text-center">
        <h1 class="display-4 font-weight-bold" style="text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);">"La educación es el
            arma más poderosa para cambiar el mundo"</h1>
        <p class="mt-3" style="font-size: 1.2rem;">- Nelson Mandela</p>
        <a asp-controller="Usuario" asp-action="Login" class="btn btn-lg btn-outline-light mt-4"
            style="border-radius: 50px; padding: 10px 30px;">Iniciar Sesión</a>
    </div>
</div>

   