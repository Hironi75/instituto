@model pf_final_ds2.Models.Estudiante

@{
    ViewData["Title"] = "Eliminar Estudiante";
}

<h1>Eliminar Estudiante</h1>

<div>
    <h4>¿Está seguro de que desea eliminar este estudiante?</h4>
    <div>
        <dl class="row">
            <dt class="col-sm-2">Nombre Completo</dt>
            <dd class="col-sm-10">@Model.NombreCompleto</dd>
            <dt class="col-sm-2">Correo</dt>
            <dd class="col-sm-10">@Model.Correo</dd>
            <dt class="col-sm-2">Teléfono</dt>
            <dd class="col-sm-10">@Model.Telefono</dd>
        </dl>
    </div>

    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger">Eliminar</button>
        <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
    </form>
</div>
